version: "3.3"
services:
  video-rtsp:
    build:
      context: ./video-rtsp/
    ports:
      - "8554:8554"
    devices:
      - "/dev/video0:/dev/video0"
      - "/dev/video1:/dev/video1"
    environment:
      frame_height: 1920
      frame_width: 1080
      video_port: 8554
      fps: 30
  gpsd:
    image: "redis:alpine"